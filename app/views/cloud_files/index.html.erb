<%= will_paginate @cloud_files %>
<ul>
  <% @cloud_files.each do |cf| %>
    <li><%= link_to("#{cf.smart_name} (#{cf.id})", cloud_file_path(cf.md5), data: { turbo: false }) %></li>
  <% end %>
</li>
<%= will_paginate @cloud_files %>
